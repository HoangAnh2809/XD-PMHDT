# Mã nguồn thực tế từ backend/chat_service/models.py
# Models for chat service (placeholder)
class ChatMessage:
    def __init__(self, sender, content, timestamp):
        self.sender = sender
        self.content = content
        self.timestamp = timestamp